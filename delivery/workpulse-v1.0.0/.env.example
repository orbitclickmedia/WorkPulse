# WorkPulse — demo delivery (no keys required for full UI demo)
# Copy to .env.local only if you run from source with `npm run dev`

NEXT_PUBLIC_APP_NAME=WorkPulse
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ORG_ID=demo-org

# Optional — leave empty for demo (mock data + simulated AI in UI)
# NEXT_PUBLIC_FIREBASE_API_KEY=
# ANTHROPIC_API_KEY=
